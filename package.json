{
  "name": "cally",
  "version": "0.5.2",
  "description": "Cally helps you making asynchronous POST requests to the server. Use it to load data or markup on the client side, while keeping users informed on what's going on.",
  "devDependencies": {
    "grunt-contrib-uglify": "~0.2.2",
    "grunt-contrib-concat": "~0.5.0"
  }
}